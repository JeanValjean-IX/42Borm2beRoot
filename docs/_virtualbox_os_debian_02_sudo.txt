instalamos sudo:

$ apt install sudo

Abriremos el fichero de configuración /etc/sudoers y 
añadiremos el usuario xxxxx.

$ nano /etc/sudoers

Añadiremos el usuario xxxxx al grupo de sudo

$ usermod -aG sudo xxxxx

Probaremos el funcionamiento. Si estamos en el usuario "root" cerraremos la sesion y
entraremos con el usuario "xxxxx", a continuacion probaremos el comando ls con sudo:

$ sudo ls

Una vez introducido el password se ejecutará el comando "ls"





